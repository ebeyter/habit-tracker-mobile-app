## Copy this file to .env and fill in real values. .env is gitignored — never commit real keys.

# Used only by scripts/generate-mascot-art.mjs (a local dev-time asset generation script).
# Never referenced at runtime by the app itself — the shipped app makes no external API calls.
FAL_KEY=
